// Rename these once the actual session names are decided — nothing else needs to change.
export const EVENTS = ["Event 1", "Event 2", "Event 3", "Event 4"] as const;

export const MIN_EVENTS_REQUIRED = 3;
